<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/windowBackground">
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/colorAccentDark"
        android:gravity="center_vertical">
        <TextView
            android:id="@+id/add_to_playlist_dialog_title"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:padding="16dp"
            android:textColor="@color/colorTextOnSecondary"
            android:fontFamily="sans-serif"
            android:textSize="18sp"
            android:text="@string/add_to_playlist"/>
        <TextView
            android:id="@+id/add_to_playlist_dialog_num_entries"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:gravity="center_vertical"
            android:padding="16dp"
            android:textColor="@color/colorTextOnSecondarySecondary"
            android:fontFamily="sans-serif"/>
    </LinearLayout>
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/add_to_playlist_dialog_recyclerview"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"/>
    <LinearLayout
        android:id="@+id/add_to_playlist_dialog_new"
        android:padding="8dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical|end"
        android:foreground="?android:selectableItemBackground"
        android:background="@color/windowBackgroundLight">
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="8dp"
            android:text="Add to new playlist"
            android:textColor="@color/text_primary_color"/>
        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_add_black_24dp"
            android:tint="@color/colorPrimaryText"
            android:contentDescription="Add to new playlist" />
    </LinearLayout>
</LinearLayout>